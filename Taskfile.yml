version: '3'

tasks:
  build:
    cmds:
      - go build -o bin/mcprt ./cmd/mcprt

  test:
    cmds:
      - go test ./...

  lint:
    cmds:
      - gofmt -l .
      - go vet ./...
      - golangci-lint run ./...

  fmt:
    cmds:
      - gofmt -w .
